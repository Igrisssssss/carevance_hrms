{
  "name": "carevance-desktop",
  "version": "1.0.12",
  "description": "CareVance Tracker desktop timer and activity tracker",
  "private": true,
  "author": "CareVance",
  "main": "main.cjs",
  "scripts": {
    "prepare:app-config": "node scripts/write-app-config.cjs",
    "start": "npm run prepare:app-config && node scripts/run-electron.cjs .",
    "pack": "npm run prepare:app-config && electron-builder --dir",
    "dist:win": "npm run prepare:app-config && electron-builder --win nsis",
    "dist:portable": "npm run prepare:app-config && electron-builder --win portable"
  },
  "build": {
    "appId": "com.carevance.tracker",
    "productName": "CareVance Tracker",
    "icon": "assets/icon.ico",
    "files": [
      "main.cjs",
      "preload.cjs",
      "app-config.json",
      "scripts/**/*",
      "assets/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Igrisssssss",
        "repo": "carevance_hrms",
        "releaseType": "release"
      }
    ],
    "asar": true,
    "npmRebuild": false,
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "assets/icon.ico",
      "artifactName": "CareVance-Tracker-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "CareVance Tracker",
      "installerIcon": "assets/icon.ico",
      "uninstallerIcon": "assets/icon.ico",
      "installerHeaderIcon": "assets/icon.ico"
    }
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "active-win": "^8.2.1",
    "electron-updater": "^6.6.2"
  }
}
